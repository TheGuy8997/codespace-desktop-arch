{
  "build": {
    "dockerfile": "Dockerfile"
  },
  "privileged": true,
  "forwardPorts": [6080],
  "runArgs": ["--cap-add=SYS_ADMIN"],
  "postCreateCommand": "pipx install numpy && mkdir -p ~/.vnc && echo 'password' | vncpasswd -f > ~/.vnc/passwd && chmod 600 ~/.vnc/passwd && git clone --depth 1 https://github.com/novnc/noVNC.git",
  "postStartCommand": "vncserver :1 -geometry 1280x800 -xstartup /usr/bin/cinnamon-session && ./noVNC/utils/novnc_proxy --vnc localhost:5901 --listen 0.0.0.0:6080"
}
